<!DOCTYPE html>
<html lang="en">
    <%- include('partials/head') %> 
<body>
    <%- include('partials/header') %>
    <main>


        <article class="article-producto">

            <div class="div-img-producto">
                <img src="/image/<%= productos.img %>">
            </div>

            <div class="div-datos-producto">

                <h4 class="h4-nombre-producto"><%= productos.name %></h4>


                <% if (productos.discount != 0) { %>

                    <div class="div-precio-con-descuento">

                        <p class="p-precio"><small>ANTES </small>$<%= toThousand(productos.price) %></p>
                        <p class="p-descuento"><%= productos.discount %>% <small> </small>OFF</p>
                        <h5 class="p-precio-final">$<%= toThousand(productos.price * (1 - productos.discount / 100)) %></h5>

                    </div>

                <% } else { %>

                    <h5 class="p-precio-final">$<%= toThousand(productos.price) %></h5> 
                    
                <% } %>

                <div class="div-formas-pago">

                    <i class="fa-solid fa-credit-card i-tarjeta"></i>
                    <span><strong>6 cuotas sin interés</strong> de $<%= toThousand((productos.price / 6).toFixed(2)) %></span>

                </div>

                <form class="form-comprar" action="/products/carritoDeCompras/<%= productos.id %>"     method="POST">
                    
                    <i class="fa-solid fa-cart-shopping i-carrito"></i>
                    <span><button class="button-agregar-al-carrito" type="submit">Agregar al carrito</button></span>
                </form>

                

                <div class="div-envio-gratis">

                    <i class="fa-solid fa-truck i-camion"></i>

                    <span><strong>Envío gratis</strong> a partir de $10.000,00</span>

                </div>

                <form action="" method="">

                    <div class="shipping-cost">
                        <input type="number" min="0" name="postal-code" placeholder="Tu código postal">

                        <button type="submit">Calcular</button>
                    </div>

                </form>

                <div class="div-descripcion-producto">

                    <p><%= productos.description%></p>
                    
                </div>

                <div class="div-botones-final">
						<a href="/products/edit/<%= productos.id %>" class="button-edit">EDITAR PRODUCTO</a>

						<form action="/products/delete/<%= productos.id %>?_method=DELETE" method="POST">
							<button type="submit" class="button-delete">ELIMINAR PRODUCTO</button>
						</form>
                </div>
            </div> 
        </article>
    
    </main>

    <%- include ('partials/footer') %>
   
</body>
</html> 